
import { metadata } from "./metadata"; // Import metadata

export { metadata };

export default function MeetourpeopleLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
